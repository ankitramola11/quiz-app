// Configuration for the Frontend
// Replace this with your actual Render or Vercel URL (e.g. 'https://quiz-app-abc.onrender.com')
// If running locally, leave it as 'http://localhost:5000' or empty string '' if frontend and backend are on same server.

export const API_BASE = 'https://quiz-app-backend-replace-me.onrender.com';
